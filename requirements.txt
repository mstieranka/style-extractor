# ---- PyTorch (CUDA) ----
# Use PyTorch's CUDA wheel index (recommended by PyTorch install instructions).
--extra-index-url https://download.pytorch.org/whl/cu113
torch>=2.3

# ---- Gradio (match your README sdk_version) ----
gradio[oauth]==6.3.0

# ---- HF stack for local inference ----
transformers==5.0.0
accelerate>=0.26,<2
huggingface-hub>=1.3.3,<2
safetensors>=0.4

# ---- Quantization for fitting larger models on 24GB ----
# bitsandbytes requires PyTorch >= 2.3.
bitsandbytes>=0.43
# Triton for optimized kernels (MXFP4, flash attention, etc.)
# Note: May not install on all systems; if it fails, the app will still work
triton>=3.4.0; platform_system == "Linux"
kernels

# ---- Tokenizers / common model deps (Gemma/Mistral often need these) ----
sentencepiece
protobuf
Pillow
